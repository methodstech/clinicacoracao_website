<link rel="stylesheet" href="~/Content/main.css" />

@using PagedList.Mvc
@model PagedList.IPagedList<AppServices.Dtos.RitmoDto>

@{
    ViewBag.Title = "Index";
}

<section id="details_ritmo" class="main">
    <div class="container">
        <div class="content">
            <header class="major">
                <h2>Ritmo</h2>
            </header>
            <section>
                <ul class="actions">
                    <li>@Html.ActionLink("Novo", "Create", null, new { @class = "button special scrolly" })</li>
                </ul>
                <div class="table-wrapper">
                    <table>
                        <thead>
                            <tr>
                                <th>Código</th>
                                <th>Descrição</th>
                                <th>Alterar</th>
                                <th>Excluir</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(modelItem => item.RitmoCodigo)</td>
                                    <td>@Html.DisplayFor(modelItem => item.RitmoDescricao)</td>
                                    <td>@Html.ActionLink("Alterar", "Edit", new { id = item.RitmoId })</td>
                                    <td>@Html.ActionLink("Excluir", "Delete", new { id = item.RitmoId })</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                @Html.PagedListPager(Model, pageNumber => Url.Action("Index", new { pageNumber }))
            </section>
        </div>
    </div>
</section>
