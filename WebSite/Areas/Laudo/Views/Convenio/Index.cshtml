<link rel="stylesheet" href="~/Content/main.css" />

@using PagedList.Mvc
@model PagedList.IPagedList<AppServices.Dtos.ConvenioDto>

@{
    ViewBag.Title = "Index";
}

<section id="details_convenio" class="main">
    <div class="container">
        <div class="content">
            <header class="major">
                <h2>Convênio</h2>
            </header>
            <section>
                <ul class="actions">
                    <li>@Html.ActionLink("Novo", "Create", null, new { @class = "button special scrolly" })</li>
                </ul>
                <div class="table-wrapper">
                    <table>
                        <thead>
                            <tr>
                                <th>Nome</th>
                                <th>Alterar</th>
                                <th>Excluir</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(modelItem => item.ConvenioNome)</td>
                                    <td>@Html.ActionLink("Alterar", "Edit", new { id = item.ConvenioId })</td>
                                    <td>@Html.ActionLink("Excluir", "Delete", new { id = item.ConvenioId })</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                @Html.PagedListPager(Model, pageNumber => Url.Action("Index", new { pageNumber }))
            </section>
        </div>
    </div>
</section>
